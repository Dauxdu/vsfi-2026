---
all:
  vars:
    ansible_python_interpreter: /usr/bin/python3
    ansible_user: dauxdu
    ansible_ssh_private_key_file: "~/.ssh/deploy_key"
  children:
    swarm_managers:
      hosts:
        mgmt1:
          ansible_host: 10.0.8.20

    app:
      hosts:
        app1:
          ansible_host: 10.0.8.24
    db:
      hosts:
        db1:
          ansible_host: 10.0.8.23
